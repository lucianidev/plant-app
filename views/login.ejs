<!DOCTYPE html>
<html lang="it" class="dark"> <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accedi - Il tuo sito (Dark Mode)</title>
    <link href="https://cdn.jsdelivr.net/npm/@preline/preline@1.0.0/dist/preline.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

    <style>
        /* Stili personalizzati aggiuntivi se necessari */
        body {
            font-family: 'Inter', sans-serif; /* Preline spesso usa Inter */
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen bg-gray-900 dark:bg-gray-900">
<main class="w-full max-w-md mx-auto p-6">
    <div class="mt-7 bg-gray-800 dark:bg-gray-800 rounded-xl shadow-lg border border-gray-700 dark:border-gray-700">
        <div class="p-4 sm:p-7">
            <div class="text-center">
                <h1 class="block text-2xl font-bold text-white dark:text-white">Accedi</h1>
                </p>
            </div>

            <div class="mt-5">
                <% if (error) { %>
                    <div class="bg-red-500 text-sm text-white rounded-md p-4 mb-4" role="alert">
                        <span class="font-bold">Errore!</span> <%= error %>
                    </div>
                <% } %>
                <form method="post" action="/users/login">
                    <div class="grid gap-y-4">
                        <div>
                            <label for="email" class="block text-sm mb-2 text-white dark:text-white">nome utente</label>
                            <div class="relative">
                                <input type="text" id="email" name="username" class="py-3 px-4 block w-full border-gray-700 rounded-md text-sm focus:border-blue-500 focus:ring-blue-500 bg-gray-900 text-gray-300 dark:bg-gray-900 dark:border-gray-700 dark:text-gray-300" required aria-describedby="email-error">
                            </div>
                        </div>
                        <div>
                            <div class="flex justify-between items-center">
                                <label for="password" class="block text-sm mb-2 text-white dark:text-white">Password</label>
                            </div>
                            <div class="relative">
                                <input type="password" id="password" name="password" class="py-3 px-4 block w-full border-gray-700 rounded-md text-sm focus:border-blue-500 focus:ring-blue-500 bg-gray-900 text-gray-300 dark:bg-gray-900 dark:border-gray-700 dark:text-gray-300" required aria-describedby="password-error">
                            </div>
                        </div>
                        <button type="submit" class="py-3 px-4 inline-flex justify-center items-center gap-2 rounded-md border border-transparent font-semibold bg-blue-600 text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-all text-sm dark:focus:ring-offset-gray-900">
                            Accedi
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/preline/dist/preline.js"></script>
</body>
</html>